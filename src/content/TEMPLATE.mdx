---
# ── Page template ──────────────────────────────────────────────
# This file is never shown on the site — it lives outside the two
# folders the site actually reads from. To publish a new piece:
#
#   1. Copy this file into ONE of:
#        src/content/work/          → shows under "Current work"
#        src/content/current-work/  → shows under "Previous work"
#      (Yes, those two folder names are confusingly similar —
#      that's just what they were called when the site was built.)
#   2. Rename the copy to a short, url-safe slug, e.g.
#        rebuilding-onboarding.mdx
#      The filename (minus .mdx) becomes the page's URL.
#   3. Fill in the fields below and write the body.
#   4. Set draft to false when you're ready for it to go live.
#
# Everything in this comment block is YAML comments — delete it or
# leave it, it's never rendered either way.
# ─────────────────────────────────────────────────────────────────

title: "A clear, specific title for the piece"
role: "Your role at the time, e.g. Head of Design"
company: "Company name"
period: "2023 — Present" # or e.g. "2021 — 2023" once it's finished
summary: "One or two sentences — this is what shows on the index card."
tags: ["tag one", "tag two", "tag three"]
metric: "Optional: the one number that matters, e.g. Grew team 3 → 14"

featured: false # true pins it near the top of its index list
draft: true # hides it from the live site entirely — flip to false to publish

# ── Password protection ─────────────────────────────────────────
# true  → visitors see a lock icon on the index card and must enter
#         the site-wide password before the full write-up is shown.
# false → open to everyone, same as any other piece.
protected: false
---

## The problem

What was broken, and why it mattered to the business, not just to designers.

## What I changed

1. **First change.** Describe it.
2. **Second change.** Describe it.
3. **Third change.** Describe it.

## What changed as a result

The before/after, ideally in numbers. If you don't have numbers, describe
the concrete behavior change instead.

## What I'd do differently

A short, honest paragraph on what you'd change if you did it again.
